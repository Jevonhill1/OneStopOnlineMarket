<h2>Categories</h2>
<%= image_tag src="onestop_logo.png" %>
<ol>
  <% @categories.each do |category| %>
    <li>
      <%= link_to category_products_path(category) do %>
        <%= category.title %>
        
      <% end %>
    </li>
  <% end %>
</ol>
